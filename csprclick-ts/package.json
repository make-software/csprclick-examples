{
  "name": "csprclick-ts",
  "version": "1.0.1",
  "description": "An empty HTML app with CSPR.click using TypeScript",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --outfile=public/app.js --format=iife --tree-shaking=false",
    "serve": "npx http-serve -p 8080 ./public"
  },
  "author": "davidatwhiletrue",
  "dependencies": {
    "@make-software/csprclick-core-types": "2.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
